package me.derpee.creelande;
import net.minecraft.item.Food;

public class ModFoods {
    public static final Food PLAUSIBLE_BURGER = (new Food.Builder()).hunger(5).saturation(0.6F).build();
    // Builds a new Food Properties variable PLAUSIBLE_BURGER with 2.5 hunger bars, and 0.6 Saturation.
}
